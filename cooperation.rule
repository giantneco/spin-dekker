never  {    /* !( [] (( outsideCriticalSection2 && X outsideCriticalSection2) -> (X (! isLocked1)))) */
T0_init:
	if
	:: ((outsideCriticalSection2)) -> goto accept_S0
	:: (1) -> goto T0_init
	fi;
accept_S0:
	if
	:: (! ((! isLocked1)) && (outsideCriticalSection2)) -> goto accept_all
	fi;
accept_all:
	skip
}
